def process_input(user_input: str) -> str:
    """Processes the user prompt before it is passed to a particular
        LLM model.
    TODO: Decide on requirements for the prompt input
        The components of this input are going to be based on what type of
        trial protocol doc is being written - each will have different logic.
    """
